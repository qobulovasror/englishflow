// Smoke test placeholder. Real tests live under `test/features/...` and
// `test/shared/...`.
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('test harness is wired up', () {
    expect(1 + 1, 2);
  });
}
